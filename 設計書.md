[Confidence: 高]

[GPT suggestion] 下面是一份可直接保存為 `DESIGN.md` 的設計書草案。
定位是 **Python + PySide6 的 Windows 桌面常駐小工具**，先做簡單可用，再預留 Confluence 同步與分享打包能力。

# 4-Line Diary Reminder App 設計書

## 1. 概要

本設計書描述一個 Windows 桌面小程式，用於每天固定時間彈出輸入視窗，提醒使用者撰寫「4行日記」。

4行日記包含以下四個項目：

* 事実
* 発見
* 教訓
* 宣言

程式以 **Python + PySide6** 開發，目標是製作一個簡單、可打包、可複用、可分享給其他 Windows 電腦使用的小工具。

---

## 2. 目的

### 2.1 主要目的

每天在指定時間彈出 Windows 桌面視窗，促使使用者輸入 4 行日記，避免忘記或拖延。

### 2.2 次要目的

* 本機保存日記內容
* 可設定提醒時間
* 可設定是否開機自動啟動
* 可選擇同步到 Confluence
* 可打包成 `.exe` 或安裝包，方便移植到其他 Windows 電腦

---

## 3. 使用場景

### 3.1 基本使用場景

1. 使用者啟動程式
2. 程式常駐於 Windows 系統匣
3. 每天到達設定時間，例如 18:30
4. 程式自動彈出輸入視窗
5. 使用者輸入：

   * 事実
   * 発見
   * 教訓
   * 宣言
6. 使用者點擊「保存」
7. 程式將內容保存到本機
8. 若啟用 Confluence 同步，則將內容 POST 到指定 Confluence Space

---

## 4. 系統定位

本程式不是大型工作流系統，而是輕量級 Windows productivity tool。

### 4.1 不採用的方案

| 方案                        | 不採用原因                       |
| ------------------------- | --------------------------- |
| Power Automate Desktop    | 可做彈窗，但不適合做可分享的小型桌面 App      |
| Power Apps                | 表單能力強，但不貼近 Windows 桌面彈窗     |
| n8n                       | API workflow 強，但不適合作為本機彈窗工具 |
| 純 Task Scheduler + script | 可用，但缺少常駐、設定、系統匣、可分享 App 體驗  |

### 4.2 採用方案

| 項目            | 選型                      |
| ------------- | ----------------------- |
| 開發語言          | Python                  |
| GUI Framework | PySide6                 |
| 常駐方式          | System Tray App         |
| 本機資料保存        | JSONL / Markdown        |
| 設定保存          | JSON                    |
| 打包方式          | PyInstaller             |
| Confluence 同步 | REST API                |
| 作業系統          | Windows 10 / Windows 11 |

---

## 5. 功能需求

## 5.1 必須功能

### F-001：系統匣常駐

程式啟動後應常駐於 Windows 系統匣。

系統匣選單包含：

* 今日を書く
* 設定
* 同期する
* 終了

---

### F-002：每日定時提醒

使用者可設定每日提醒時間。

範例：

```text
18:30
```

程式應在每日指定時間彈出日記輸入視窗。

---

### F-003：4行日記輸入

輸入視窗包含四個欄位：

| 欄位 | 說明            |
| -- | ------------- |
| 事実 | 今天實際發生的事情     |
| 発見 | 今天發現或注意到的事情   |
| 教訓 | 今天得到的教訓       |
| 宣言 | 明天或之後要採取的行動宣言 |

---

### F-004：本機保存

日記內容必須先保存到本機。

保存格式建議使用 JSONL。

範例：

```json
{"date":"2026-06-17","fact":"...","discovery":"...","lesson":"...","declaration":"...","created_at":"2026-06-17T18:35:00+09:00","sync_status":"pending"}
```

---

### F-005：重複提醒控制

若使用者沒有提交，應允許延後提醒。

延後選項：

* 5 分後
* 10 分後
* 30 分後

建議限制每日最多延後 3 次。

---

### F-006：設定畫面

設定畫面包含：

| 設定項目                      | 說明                      |
| ------------------------- | ----------------------- |
| reminder_time             | 每日提醒時間                  |
| popup_mode                | 彈窗強度                    |
| max_snooze_count          | 每日最大延後次數                |
| confluence_enabled        | 是否啟用 Confluence 同步      |
| confluence_base_url       | Confluence URL          |
| confluence_space_id       | Space ID                |
| confluence_parent_page_id | Parent Page ID          |
| confluence_email          | Atlassian account email |
| confluence_api_token      | API Token               |
| autostart_enabled         | 是否開機自動啟動                |

---

## 5.2 可選功能

### F-101：Confluence 同步

若啟用 Confluence，同步時將每日日記建立為一個 Confluence Page。

頁面標題格式：

```text
4行日記 - YYYY-MM-DD
```

頁面內容格式：

```html
<h1>4行日記 - YYYY-MM-DD</h1>

<h2>事実</h2>
<p>...</p>

<h2>発見</h2>
<p>...</p>

<h2>教訓</h2>
<p>...</p>

<h2>宣言</h2>
<p>...</p>
```

---

### F-102：同步失敗重試

若 Confluence API 發送失敗：

1. 本機資料不得丟失
2. 該筆資料標記為 `sync_status = failed`
3. 系統匣選單提供「同期する」手動重試

---

### F-103：每日防重複

同一天已經提交過日記時，再次觸發提醒時應提示：

```text
今日の4行日記は既に保存されています。
再編集しますか？
```

---

### F-104：Markdown 匯出

可將本機日記匯出為 Markdown。

範例：

```markdown
# 4行日記 - 2026-06-17

## 事実

...

## 発見

...

## 教訓

...

## 宣言

...
```

---

## 6. 非功能需求

## 6.1 可移植性

程式應可打包為 `.exe`，並可在其他 Windows 電腦上執行。

目標部署形式：

```text
DailyDiary/
├─ DailyDiary.exe
├─ config.json
├─ data/
├─ logs/
└─ README.md
```

---

## 6.2 易用性

初次使用流程應盡量簡單：

1. 啟動 `DailyDiary.exe`
2. 設定提醒時間
3. 選擇是否啟用 Confluence
4. 完成

---

## 6.3 安全性

API Token 不建議明文保存。

初版 MVP 可先保存於本機設定檔，但正式版建議使用：

* Windows Credential Manager
* 或本機加密保存

---

## 6.4 穩定性

即使 Confluence 同步失敗，也不得影響本機保存。

保存順序應為：

```text
先保存本機
再嘗試同步 Confluence
```

---

## 6.5 低依賴

程式應盡量減少外部服務依賴。

最低可用狀態：

```text
不設定 Confluence 也可以正常作為本機日記提醒工具使用
```

---

# 7. 系統架構

## 7.1 架構圖

```text
+-----------------------------+
| Windows User Session         |
+-----------------------------+
              |
              v
+-----------------------------+
| DailyDiary Tray App          |
| Python + PySide6             |
+-----------------------------+
  |             |             |
  v             v             v
Reminder     Diary UI      Settings UI
Scheduler    Dialog        Dialog
  |             |             |
  +-------------+-------------+
                |
                v
+-----------------------------+
| Local Storage                |
| JSONL / Markdown / config   |
+-----------------------------+
                |
                v
+-----------------------------+
| Optional Sync Service        |
| Confluence REST API          |
+-----------------------------+
```

---

## 7.2 模組構成

```text
daily_diary/
├─ main.py
├─ app/
│  ├─ tray.py
│  ├─ reminder.py
│  ├─ diary_dialog.py
│  ├─ settings_dialog.py
│  ├─ storage.py
│  ├─ confluence_client.py
│  ├─ config.py
│  ├─ autostart.py
│  └─ logger.py
├─ data/
├─ logs/
├─ config.json
└─ requirements.txt
```

---

# 8. 模組設計

## 8.1 main.py

### 責任

* 啟動 QApplication
* 初始化設定
* 初始化系統匣
* 初始化提醒 scheduler
* 啟動主事件迴圈

---

## 8.2 tray.py

### 責任

* 建立 Windows 系統匣圖示
* 建立右鍵選單
* 接收使用者操作

### Tray Menu

```text
今日を書く
設定
同期する
終了
```

---

## 8.3 reminder.py

### 責任

* 監控目前時間
* 判斷是否到達提醒時間
* 控制 snooze
* 避免同一天重複彈出

### 判斷邏輯

```text
if current_time >= reminder_time
and today_not_submitted
and not_already_triggered_today:
    show_diary_dialog()
```

---

## 8.4 diary_dialog.py

### 責任

* 顯示 4行日記輸入視窗
* 驗證輸入內容
* 觸發保存事件

### UI 欄位

| 元件          | 說明       |
| ----------- | -------- |
| QTextEdit   | 事実       |
| QTextEdit   | 発見       |
| QTextEdit   | 教訓       |
| QTextEdit   | 宣言       |
| QPushButton | 保存       |
| QPushButton | 10分後に再通知 |
| QPushButton | キャンセル    |

---

## 8.5 settings_dialog.py

### 責任

* 顯示設定畫面
* 保存設定
* 測試 Confluence 連線

---

## 8.6 storage.py

### 責任

* 保存日記
* 讀取日記
* 更新同步狀態
* 檢查今天是否已提交

### 建議資料格式

JSONL：

```text
data/diary_2026.jsonl
```

每一行為一筆日記紀錄。

---

## 8.7 confluence_client.py

### 責任

* 建立 Confluence Page
* 測試 API 連線
* 處理 API Error
* 回傳同步結果

### 主要方法

```python
create_diary_page(diary_entry) -> SyncResult
test_connection() -> bool
```

---

## 8.8 config.py

### 責任

* 讀取 `config.json`
* 寫入 `config.json`
* 提供預設值

### config.json 範例

```json
{
  "reminder_time": "18:30",
  "popup_mode": "force",
  "max_snooze_count": 3,
  "snooze_minutes": [5, 10, 30],
  "local_save_dir": "./data",
  "autostart_enabled": false,
  "confluence": {
    "enabled": false,
    "base_url": "",
    "space_id": "",
    "parent_page_id": "",
    "email": "",
    "api_token": ""
  }
}
```

---

## 8.9 autostart.py

### 責任

* 設定 Windows 開機自動啟動
* 移除開機自動啟動

### 實作方式

初版可使用 Windows Registry：

```text
HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run
```

---

# 9. UI 設計

## 9.1 日記輸入視窗

```text
┌────────────────────────────────────┐
│ 今日の4行日記                       │
├────────────────────────────────────┤
│ 事実                                │
│ [                                  ]│
│                                    │
│ 発見                                │
│ [                                  ]│
│                                    │
│ 教訓                                │
│ [                                  ]│
│                                    │
│ 宣言                                │
│ [                                  ]│
├────────────────────────────────────┤
│ [保存] [10分後に再通知] [キャンセル] │
└────────────────────────────────────┘
```

---

## 9.2 設定畫面

```text
┌────────────────────────────────────┐
│ 設定                                │
├────────────────────────────────────┤
│ Reminder Time       [18:30      ]   │
│ Popup Mode          [Force      ]   │
│ Max Snooze Count    [3          ]   │
│                                    │
│ □ Enable Confluence Sync            │
│ Base URL            [           ]   │
│ Space ID            [           ]   │
│ Parent Page ID      [           ]   │
│ Email               [           ]   │
│ API Token           [********   ]   │
│                                    │
│ □ Start on Windows login            │
├────────────────────────────────────┤
│ [Test Connection] [Save] [Cancel]   │
└────────────────────────────────────┘
```

---

# 10. 彈窗強度設計

## 10.1 Popup Mode

| Mode   | 說明             |
| ------ | -------------- |
| gentle | 只顯示通知          |
| normal | 彈出視窗，可關閉       |
| force  | 彈出置頂視窗，未填寫只能延後 |
| strict | 延後次數用完後必須填寫    |

初版建議實作：

```text
normal
force
```

---

## 10.2 Force Mode 行為

Force Mode 下：

* 視窗置頂
* 未填寫時不允許保存
* 關閉時顯示確認
* 可選擇延後提醒
* 延後次數受限制

---

# 11. 資料設計

## 11.1 DiaryEntry

```json
{
  "id": "2026-06-17",
  "date": "2026-06-17",
  "fact": "今天完成了...",
  "discovery": "我發現...",
  "lesson": "我學到...",
  "declaration": "明天我要...",
  "created_at": "2026-06-17T18:35:00+09:00",
  "updated_at": "2026-06-17T18:35:00+09:00",
  "sync_status": "pending",
  "confluence_page_id": null,
  "sync_error": null
}
```

---

## 11.2 sync_status

| 狀態      | 說明    |
| ------- | ----- |
| none    | 未啟用同步 |
| pending | 等待同步  |
| synced  | 已同步   |
| failed  | 同步失敗  |

---

# 12. Confluence 同步設計

## 12.1 建頁策略

建議每日建立一個獨立頁面。

```text
4行日記
└─ 2026
   └─ 2026-06
      ├─ 4行日記 - 2026-06-17
      ├─ 4行日記 - 2026-06-18
      └─ 4行日記 - 2026-06-19
```

初版可以簡化為：

```text
指定 Parent Page
└─ 4行日記 - YYYY-MM-DD
```

---

## 12.2 同步流程

```text
使用者保存日記
        ↓
寫入本機 JSONL
        ↓
檢查是否啟用 Confluence
        ↓
若未啟用：sync_status = none
        ↓
若啟用：POST Confluence Page
        ↓
成功：sync_status = synced
失敗：sync_status = failed
```

---

## 12.3 失敗處理

若同步失敗：

* 保留本機資料
* 記錄錯誤訊息
* 系統匣顯示「同步失敗」
* 使用者可手動點擊「同期する」重試

---

# 13. 打包與部署

## 13.1 requirements.txt

```text
PySide6
requests
```

---

## 13.2 開發啟動

```bash
python main.py
```

---

## 13.3 打包

使用 PyInstaller：

```bash
pyinstaller --noconsole --onefile --name DailyDiary main.py
```

輸出：

```text
dist/DailyDiary.exe
```

---

## 13.4 Portable 版資料夾

```text
DailyDiary/
├─ DailyDiary.exe
├─ config.json
├─ data/
├─ logs/
└─ README.md
```

---

# 14. 錯誤處理

## 14.1 本機保存失敗

處理方式：

* 顯示錯誤對話框
* 寫入 log
* 不關閉輸入視窗

---

## 14.2 Confluence 同步失敗

處理方式：

* 日記仍保存成功
* 顯示同步失敗提醒
* 設定 `sync_status = failed`
* 可手動重試

---

## 14.3 設定檔損壞

處理方式：

* 備份損壞設定檔
* 重新建立預設設定
* 顯示提醒

---

# 15. 日誌設計

日誌檔案：

```text
logs/app.log
```

記錄內容：

* App start
* App exit
* Reminder triggered
* Diary saved
* Confluence sync success
* Confluence sync failed
* Config changed
* Unexpected error

---

# 16. MVP 範圍

## 16.1 MVP 必做

* PySide6 App 啟動
* System Tray
* 設定提醒時間
* 到時間彈出 4行日記視窗
* 保存到本機 JSONL
* 手動打開「今日を書く」
* 設定畫面
* 開機自動啟動設定

---

## 16.2 MVP 不做

* Confluence 自動建月份父頁
* 自動更新
* 複雜統計
* 手機同步
* 多使用者管理
* 雲端資料庫

---

# 17. 開發階段規劃

## Phase 1：本機可用版

目標：可以每天彈窗並保存本機日記。

功能：

* Tray App
* Reminder
* Diary Dialog
* Local JSONL Save
* Settings Dialog

---

## Phase 2：Windows 整合版

目標：更像正式 Windows 小工具。

功能：

* 開機自動啟動
* Force Popup Mode
* Snooze
* Log
* Portable Folder Structure

---

## Phase 3：Confluence 同步版

目標：可將日記同步到 Confluence。

功能：

* Confluence API 設定
* Test Connection
* Create Page
* Sync Retry
* Sync Status

---

## Phase 4：分享版

目標：可分享給其他電腦使用。

功能：

* PyInstaller 打包
* README
* 初次設定 Wizard
* 設定匯入 / 匯出
* 更安全的 API Token 保存

---

# 18. 風險與對策

| 風險                | 影響         | 對策                                            |
| ----------------- | ---------- | --------------------------------------------- |
| App 沒有常駐          | 無法準時提醒     | 使用系統匣常駐，並支援開機啟動                               |
| 電腦睡眠              | 無法彈窗       | 可搭配 Windows Task Scheduler 喚醒，但 MVP 不保證睡眠狀態提醒 |
| Confluence API 失敗 | 無法同步       | 本機先保存，之後手動重試                                  |
| API Token 洩漏      | 安全風險       | 正式版改用 Windows Credential Manager              |
| 使用者直接關閉視窗         | 無法強制寫      | Force Mode 限制關閉，提供 Snooze                     |
| 打包後路徑錯誤           | App 無法讀寫資料 | 使用 AppData 或 exe 所在目錄作為資料目錄                   |

---

# 19. 驗收標準

## 19.1 基本驗收

* 啟動後可在系統匣看到圖示
* 可從系統匣打開「今日を書く」
* 可輸入 4 個欄位並保存
* 保存後本機 JSONL 產生資料
* 設定提醒時間後，到時間會彈窗
* 關閉主視窗不會結束 App
* 點擊系統匣「終了」才會結束 App

---

## 19.2 進階驗收

* 可設定開機自動啟動
* 可延後提醒
* 延後次數限制有效
* Confluence 設定正確時可建立頁面
* Confluence 失敗時本機資料不丟失
* 打包後 `.exe` 可在其他 Windows 電腦執行

---

# 20. 結論

本程式應以「小而穩」為第一原則。

第一版不應追求完整自動化平台，而應先完成以下核心閉環：

```text
每天定時彈窗
        ↓
輸入 4 行日記
        ↓
本機保存
        ↓
可選同步 Confluence
```

使用 Python + PySide6 可以快速完成 Windows 桌面 UI、系統匣、設定畫面與本機資料保存。
後續若需要產品化，可再加上 Confluence 同步、開機自動啟動、API Token 安全保存與 installer 打包。

[GPT inference]
是的，**這個小程式本身不難**。真正需要注意的不是 UI，而是這幾個細節：

1. **常駐與定時觸發要穩**
2. **本機資料一定要先保存**
3. **Confluence 同步失敗不能影響日記保存**
4. **API Token 不要長期明文保存**
5. **打包後路徑處理要設計好**

[GPT suggestion] 下一步最合理是直接做 **MVP 程式骨架**：`main.py + tray.py + diary_dialog.py + storage.py + config.py`。

---

# 21. 実装版 補遺（現状反映 / 2026-06-17）

本章は、上記の原案（草案）を実装する過程で確定・追加・変更した内容を反映したものです。
原案と差異がある場合は**本章を優先**します。

## 21.1 実装ステータス

| フェーズ | 内容 | 状況 |
| ---- | ---- | ---- |
| Phase 1 | トレイ常駐 / 定時リマインダー / 4行入力 / JSONL保存 / 設定 / 自動起動 | 完了 |
| Phase 2 | Force モード / スヌーズ / ログ / 常時最前面 / 多重起動防止 | 完了 |
| Phase 3 | Confluence 同期（接続テスト / 作成・更新 upsert / 失敗リトライ / 月次親ページ自動作成）| 完了 |
| Phase 4 | 設定2層化 / 初回ウィザード / API Token を資格情報マネージャー保存 | 完了 |
| Phase 4 | PyInstaller での .exe 化（`build.bat`、--noconsole --onefile）| 完了 |

## 21.2 実際のモジュール構成（原案 7.2 を更新）

原案に加えて以下を追加した。

```text
app/
├─ main.py              起動・全体制御（AppController）
├─ tray.py              トレイ常駐・メニュー
├─ reminder.py          定時監視・スヌーズ・1日1回制御
├─ diary_dialog.py      4行入力ウィンドウ（★再利用する非モーダル窓に変更）
├─ settings_dialog.py   設定画面（接続テスト付き）
├─ first_run.py         ★初回設定ウィザード
├─ storage.py           JSONL 保存（年別・同日 upsert・未同期抽出）
├─ confluence_client.py Confluence v2 API（gateway 経由・upsert・月次親）
├─ sync_worker.py       ★同期をバックグラウンドスレッド(QThread)で実行
├─ secrets_store.py     ★API Token を資格情報マネージャー(keyring)へ
├─ config.py            2層設定の読み書き（差分保存・トークン注入）
├─ single_instance.py   ★多重起動防止（QLocalServer）
├─ autostart.py         ログオン自動起動（レジストリ）
├─ icon.py              ★トレイアイコンをコード内描画
├─ paths.py             ★Portable パス解決
└─ logger.py            ログ（logs/app.log）
```

## 21.3 日記ウィンドウの設計変更（原案 8.4 / 10 を更新）

- `QDialog.exec()` のモーダル方式をやめ、**アプリ起動中は1つだけ使い回す非モーダル窓**に変更。
  - 理由：再表示要求が重なると窓が積み重なり、閉じた後に裏の窓が出てくる不具合が出たため。
- ボタン操作は Qt シグナル（saved / snoozed / cancelled）で通知。
- 窓は normal/force いずれでも**常に最前面**（リマインダーとして確実に見せるため）。
- Force モードでは未保存のまま閉じようとすると確認し、再通知へ誘導する。
- 設定画面も最前面（日記窓の下に隠れないように）。

## 21.4 多重起動防止・手動起動の挙動（追加仕様）

- `QLocalServer/QLocalSocket` による単一インスタンス制御。
- 2回目以降の起動は新プロセスを作らず、**既存インスタンスに日記窓の表示を依頼**して終了。
- `run.bat` / `run_debug.bat` は `--show` 付きで起動し、手動起動時は日記窓を開く。
  ログオン自動起動（レジストリ）は `--show` 無し＝静かに常駐。

## 21.5 設定モデル（原案 8.8 を更新：2層構成 + 資格情報マネージャー）

優先順位（後ほど上書き）：

```text
1. DEFAULT_CONFIG（コード内既定値）
2. config.default.json（管理者デフォルト・任意・非秘密）
3. config.json（個人の上書き・差分のみ保存・非秘密）
4. API Token … Windows 資格情報マネージャー（keyring, service="DailyDiary"）
```

- `config.json` には**管理者デフォルトと異なる項目だけ**を保存（疎な差分）。
- API Token は**ファイルに平文保存しない**。旧 `config.json` の平文トークンは
  次回起動時に自動で資格情報マネージャーへ移行し、ファイルから削除する。
- これにより原案 6.3（セキュリティ）の「正式版で Credential Manager」を**実装済み**。

## 21.6 初回設定ウィザード（追加仕様）

- `config.json` が無い初回起動時にウィザードを表示。
- `config.default.json` で接続先（base_url / space_id）が用意済みなら、利用者は
  **Parent Page ID・Email・API Token を入力するだけ**で同期可能（新人配布向け）。
- 用意が無ければ全項目入力可（個人利用向け）。

## 21.7 Confluence 連携の実装詳細（原案 12 を更新）

- **API**：Confluence Cloud REST API **v2**（`/api/v2/pages` 等）。
- **認証経路（重要）**：**スコープ付き API Token** はサイト直 URL
  (`<site>/wiki/api/v2/...`) では 401 になる。`<site>/_edge/tenant_info` で
  cloudId を取得し、**`https://api.atlassian.com/ex/confluence/{cloudId}/api/v2/...`
  ゲートウェイ経由**で Basic 認証する。
- **必要スコープ**：`write:page:confluence` / `read:page:confluence` / `read:space:confluence`
  （blogpost ではなく page）。
- **upsert**：保存済み `confluence_page_id`、無ければ同名タイトル検索で既存ページを特定し、
  **あれば更新（PUT・バージョン+1）／無ければ作成（POST）**。同名ページの重複作成エラーを回避。
- **月次親ページ自動作成（任意 `monthly_parent`）**：`parent_page_id` をルートとして
  `YYYY` → `YYYY-MM` を探し、無ければ作成し、その下に日次ページを置く。既存は再利用。
- **非同期**：同期はバックグラウンドスレッドで実行し UI を固めない。
- **失敗時**：本機保存は維持し `sync_status=failed`、トレイ「同期する」で再送（原案 14.2 準拠）。
- **アクセス制御**：利用者が書ける範囲は **Confluence 側のスペース/ページ権限**で管理する
  （アプリ側での親ページ強制は行わない方針）。

## 21.8 環境・運用メモ

- **Python 仮想環境**：`%USERPROFILE%\ddvenv`。OneDrive 配下の長パス + Microsoft Store 版
  Python では PySide6 インストール時にパス長制限(WinError 206)に当たるため、短いパスに作成。
- **.bat ファイル**：CRLF 改行 + ASCII コメントで作成（LF / 日本語コメントだと cmd で構文エラー）。
- **ソース管理**：GitHub `T-King-Sawada/daily-diary`（private）。
  `config.json` / `config.default.json` / `config.bak*` / `data/` / `logs/` は
  `.gitignore` で除外（秘密・個人データ・環境固有のため）。`config.example.json` をテンプレートとして同梱。

## 21.9 残課題

- 配布フォルダの整備・運用（管理者向け `config.default.json` の用意、インストーラ化の検討）。
- 必要に応じて：年跨ぎ時の月次親ページ動作確認、自動更新の仕組み。

> `.exe` 化は完了（`build.bat` で `dist/DailyDiary.exe` を生成。起動・トレイ常駐・
> 資格情報マネージャー動作を確認済み）。
